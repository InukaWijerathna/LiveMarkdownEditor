# LiveMarkdownEditor

Lightweight split-screen Markdown editor with an editable preview.

Project structure:

- `index.html` — entry HTML that loads assets from `src/`
- `src/css/styles.css` — extracted styles
- `src/js/app.js` — application logic

Open `index.html` in a browser to run.
